Archived legacy code

This folder is a placeholder to note that earlier iterations included a Vite client (`client/`) and an Express server (`server/`).

Those folders have been removed to avoid confusion. If you need to access them, use git history at the commit prior to this cleanup.

- Legacy Vite client: `client/`
- Legacy Express server: `server/`

The active application is the Next.js app under `src/` with API routes in `src/app/api`.

