# Static Copy Inventory

## 1. Home Page (`src/pages/homes/home-1.tsx`)

1.1 **Hero headline & supporting text** (`src/components/hero-banner/hero-banner-one.tsx`)
   - current text: "We’re / VONAS Media / Bold digital channel builders partnering with creators and brands to make content with impact. We build formats that scale—stories that resonate like culture itself."
   - change to:

1.2 **Hero subtitle overlay** (`src/components/hero-banner/hero-banner-one.tsx`)
   - current text: "Empowering Creators and Brands"
   - change to:

1.3 **Hero video caption** (`src/components/video/video-one.tsx`)
   - current text: *(none – video only)*
   - change to:

1.4 **Brand section title** (`src/components/brand/brand-one.tsx`)
   - current text: "Brands We Partner With"
   - change to:

1.5 **Service section heading** (`src/components/service/service-one.tsx`)
   - current text: "Shows / Formats"
   - change to:

1.6 **Service CTA button** (`src/components/service/service-one.tsx`)
   - current text: "Explore Our Shows"
   - change to:

1.7 **Service descriptions** (`src/components/service/service-one.tsx`)
   - current text: "We launch digital shows that build loyal audiences and drive real engagement across platforms." / "In-house talent management supporting 20+ creators with growth strategies and brand partnerships." / "We engineer repeatable show formats—from concept to execution—that scale across channels." / "Partner with brands to create authentic content that connects with Filipino audiences."
   - change to:

1.8 **Project marquee text** (`src/components/project/project-text-line.tsx`)
   - current text: repeated "Our Channels"
   - change to:

1.9 **Project CTA button** (`src/components/project/project-one.tsx`)
   - current text: "View All Shows"
   - change to:

1.10 **Award section heading** (`src/components/award/award-one.tsx`)
   - current text: "Awards & Recognition"
   - change to:

1.11 **Award entries** (`src/components/award/award-one.tsx`)
   - current text: lines like "x2 FWA, Site of the Day — Jun 24, 2024" etc. (six total)
   - change to:

1.12 **Testimonial quotes** (`src/components/testimonial/testimonial-one.tsx`)
   - current text: Maria Santos quote; David Kim quote
   - change to:

1.13 **Footer CTA circle** (`src/pages/homes/home-1.tsx`)
   - current text: *(no visible text)*
   - change to:

## 2. About Page (`src/pages/about/about-us.tsx`)

2.1 **Hero subtitle & title** (`src/components/about/about-us-hero.tsx`)
   - current text: "VONAS Media / Building Creator Culture"
   - change to:

2.2 **Hero supporting tagline** (`src/components/about/about-us-hero.tsx`)
   - current text: "Digital content with lasting cultural impact"
   - change to:

2.3 **Hero CTA button** (`src/components/about/about-us-hero.tsx`)
   - current text: "Our Journey"
   - change to:

2.4 **Intro paragraph** (`src/components/about/about-us-area.tsx`)
   - current text: "We are a content-first media lab connecting Philippine creators with brands. From talent management to show production, we’re your strategic partner in the creator economy!"
   - change to:

2.5 **What We Do heading** (`src/components/about/about-us-area.tsx`)
   - current text: "Our Services / WHAT WE DO"
   - change to:

2.6 **What We Do list – column 1** (`src/components/about/about-us-area.tsx`)
   - current text: "Channel Building; Content Strategy; Creator Support; Format Engineering; Video Production"
   - change to:

2.7 **What We Do list – column 2** (`src/components/about/about-us-area.tsx`)
   - current text: "Brand Storytelling; Documentary Shorts; Cultural Research; Platform Strategy"
   - change to:

2.8 **Fun Facts heading** (`src/components/fun-fact/fun-fact-one.tsx`)
   - current text: "Fun Facts / Agency Snapshots"
   - change to:

2.9 **Fun Fact counters** (`src/components/fun-fact/fun-fact-one.tsx`)
   - current text: "200+ Projects Delivered; 5+ Years of Excellence; 9 Team Members Strong; 194% Agency Growth Rate"
   - change to:

2.10 **Brand section title** (`src/components/brand/brand-five.tsx` if static text present)
   - current text: *(confirm once reviewed)*
   - change to:

## 3. Channels Page (`src/app/channels/page.tsx` → `src/pages/homes/home-7.tsx`)

3.1 **Panel headings & subtitles** (`src/components/studio-panels/studio-panel-1.tsx` … `studio-panel-5.tsx`)
   - current text: *(capture each panel’s title, subtitle, and paragraph once reviewed)*
   - change to:

3.2 **Panel CTA buttons** (`src/components/studio-panels/*.tsx`)
   - current text: e.g., "Explore Formats", "Meet the Team" (verify actual strings)
   - change to:

3.3 **Channel gallery fallback copy** (`src/components/project/project-one.tsx`)
   - current text: fallback label uses channel name or "Channel"
   - change to:

## 4. Creators Section/Pages (`src/app/creators/[slug]/page.tsx` etc.)

4.1 **Fallback message** (`src/components/creator-detail/creator-details-area.tsx`)
   - current text: "Creator not found"
   - change to:

4.2 **Detail section headings** (`src/components/creator-detail/creator-details-right-wrap.tsx`)
   - current text: *(enumerate headings like “Overview”, “Highlights”, etc., after reviewing file)*
   - change to:

## 5. Contact Page (`src/app/contact/page.tsx`)

5.1 **Hero heading & paragraph** (identify component used on contact page)
   - current text: *(pending review)*
   - change to:

5.2 **Form labels and button** (`src/components/contact/...`)
   - current text: e.g., "Name", "Email", "Message", "Send Message" (confirm actual strings)
   - change to:

## 6. FAQ Page (`src/app/faq/page.tsx`)

6.1 **FAQ hero copy**
   - current text: *(pending review)*
   - change to:

6.2 **Questions & answers** (`src/components/faq/...`)
   - current text: list each Q/A pair once confirmed
   - change to:

---

Continue appending numbered items for any additional pages (e.g., `pricing`, `shop`, `news`, `docs`) following the same `current text / change to` structure once their static strings are captured.
